/** Campagna di riferimento per il benchmark immagini (memoria IA + paletti). */
export const BENCHMARK_CAMPAIGN_NAME = "Le Cronache di Eldaria";

/** Pattern di ricerca flessibile (case-insensitive) sul nome campagna. */
export const BENCHMARK_CAMPAIGN_NAME_PATTERN = "%cronache di eldaria%";
