"use client";
import { Check, Edit3, RotateCcw, Save, Trash2 } from "lucide-react";
import { Button } from "@/components/ui/button";
import type { AiAssistantArtifact } from "@/modules/command-center/ai-v2/contracts";
export function AiAssistantV2ArtifactCard({ artifact, prepared, onEdit, onRegenerate, onDiscard, onPrepare, onConfirm }: { artifact: AiAssistantArtifact; prepared: boolean; onEdit: (content: string) => void; onRegenerate: () => void; onDiscard: () => void; onPrepare: () => void; onConfirm: () => void }) { const content = typeof artifact.payload.content === "string" ? artifact.payload.content : ""; return <article className="rounded-2xl border border-barber-gold/25 bg-barber-dark/70 p-4 shadow-[0_12px_35px_rgba(0,0,0,.18)]"><div className="mb-3 flex flex-wrap items-center justify-between gap-2"><div><p className="text-[10px] font-semibold uppercase tracking-[0.18em] text-barber-gold">Bozza · revisione {artifact.revision}</p><h3 className="mt-1 font-serif text-lg text-barber-paper">{String(artifact.payload.title ?? "Artefatto narrativo")}</h3></div><span className="rounded-full border border-amber-400/20 bg-amber-400/10 px-2 py-1 text-[10px] uppercase tracking-wide text-amber-200">{prepared ? "Pronta per conferma" : "Non salvato"}</span></div>{typeof artifact.payload.imageUrl === "string" ? <img src={artifact.payload.imageUrl} alt={String(artifact.payload.title ?? "Immagine generata")} className="mb-3 aspect-square w-full rounded-xl object-cover" /> : null}<textarea aria-label="Contenuto della bozza" value={content} onChange={(e) => onEdit(e.target.value)} className="min-h-40 w-full resize-y rounded-xl border border-white/10 bg-black/15 p-3 text-sm leading-6 text-barber-paper outline-none transition focus:border-barber-gold/50" /><div className="mt-3 flex flex-wrap gap-2"><Button size="sm" variant="outline" onClick={() => onEdit(content)}><Edit3 className="mr-1.5 h-3.5 w-3.5" />Modifica</Button><Button size="sm" variant="outline" onClick={onRegenerate}><RotateCcw className="mr-1.5 h-3.5 w-3.5" />Rigenera</Button><Button size="sm" variant="outline" onClick={onDiscard}><Trash2 className="mr-1.5 h-3.5 w-3.5" />Scarta</Button><Button size="sm" variant="outline" onClick={onPrepare}><Save className="mr-1.5 h-3.5 w-3.5" />Prepara salvataggio</Button><Button size="sm" onClick={onConfirm} disabled={!prepared}><Check className="mr-1.5 h-3.5 w-3.5" />Conferma e salva</Button></div></article> }
