import { ExternalLink, Library } from "lucide-react";
import type { AiAssistantSourceRef } from "@/modules/command-center/ai-v2/contracts";
export function AiAssistantV2Sources({ sources }: { sources: AiAssistantSourceRef[] }) { return <section className="rounded-xl border border-white/[0.07] bg-black/10 p-3"><div className="mb-2 flex items-center gap-2 text-xs font-medium uppercase tracking-[0.14em] text-barber-paper/45"><Library className="h-3.5 w-3.5" /> Fonti della campagna</div>{sources.length ? <ul className="space-y-1.5">{sources.map((s) => <li key={s.evidenceId}><a href={s.href} target="_blank" rel="noreferrer" className="group flex items-center justify-between gap-2 text-sm text-barber-paper/75 hover:text-barber-gold"><span><span className="mr-2 font-mono text-xs text-barber-gold">[{s.evidenceId}]</span>{s.title}</span><ExternalLink className="h-3 w-3 opacity-0 group-hover:opacity-100" /></a></li>)}</ul> : <p className="text-sm text-barber-paper/45">Nessuna fonte canonica trovata. Le aggiunte saranno proposte creative.</p>}</section> }
